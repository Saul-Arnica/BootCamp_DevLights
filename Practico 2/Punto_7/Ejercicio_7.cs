/*
 * 7. Almacenar en una matriz las tablas del 1 al 9, 
 * teniendo en cuenta que en la primera fila y la primera columna se debe guardar los números (de 0 a 9),
 * estando el cero en la primera posición (fila 0, columna 0). 
 * El resto de los lugares debe ser calculado usando los números que se dispone,
 * por ejemplo, en la fila 1, calcular 1*1, 1*2, 1*3, etc. usando las posiciones del array o arreglo. 
 * Al finalizar el cálculo, mostrar la matriz por pantalla
 */

using System;
using System.Collections.Generic;

namespace Punto_7;

public class Ejercicio_7
{
    public static void Main(/*string[] args*/)
    {

    }
}